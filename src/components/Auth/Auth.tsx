import React from 'react';
import Title from '../Atoms/Title/Title.tsx';

const Auth: React.FC = () => {
    return (
        <div>
            <Title size={'large'} variant={'secondary'}>
                Login
            </Title>
        </div>
    );
};

export default Auth;
